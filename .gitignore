# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 환경변수 (Supabase 키 등 — git 커밋 방지)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Playwright MCP 산출물(스크린샷·콘솔 로그)
.playwright-mcp/

# Playwright
/test-results
/playwright-report
e2e/.auth

# Supabase CLI 로컬 메타데이터
supabase/.temp/

# SQL 마이그레이션은 저장소에 노출하지 않음(보안). Supabase 에만 적용.
*.sql

# 별도 저장소(자체 .git) — aidev-home 커밋에 포함하지 않음
gdoc-fixer/
